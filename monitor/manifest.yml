---
applications:
- name: autoscale-monitor
  memory: 512M
  instances: 1
  host: autoscale-monitor
  domain: tempest-stine.com
  path: build/libs/monitor.jar
  services:
    rabbitmq:
      label: p-rabbitmq
      provider: pivotal
      version: '1.0'
      plan: Standard
